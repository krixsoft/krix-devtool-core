**krix/devtool-core** - core-package which provides shared interfaces, enums, constants and etc for Krix DevTool packages. It's an independent part of Krix ecosystem.

# Installation
```
npm install -S @krix/devtool-core
```
